# Automatic HTTPS via Let's Encrypt for the deploy domain (service.layout.ai).
# Requires: DNS A record -> droplet IP, ports 80/443 open, and the Cloudflare record
# set to "DNS only" (grey cloud) so Caddy can complete the HTTP-01 challenge.
{$DOMAIN} {
	encode gzip
	reverse_proxy app:8080
}
